// Chakra exposes this component as `Switch`. Our design system surfaces
// it as `Toggle` — re-export the namespace under that name so the dotted
// access works (`Toggle.Root`, `Toggle.RootProps`, ...).
export { Switch as Toggle } from '@chakra-ui/react';

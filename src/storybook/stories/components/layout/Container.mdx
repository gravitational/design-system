import { Meta } from '@storybook/addon-docs/blocks';

import { Container } from '../../../..';

import { Canvas } from '../../../components/Canvas';
import { DocsHeader } from '../../../components/DocsHeader';
import { PropTypes } from '../../../components/PropTypes';

import * as ContainerStories from './Container.stories';

<Meta title="Components/Layout/Container" />

<DocsHeader
  title="Container"
  description="Used to constrain a content's width to the current breakpoint, while keeping it fluid."
  recipePath="src/components/container/recipe.ts"
/>

<Canvas of={ContainerStories.Preview} />

## Usage

The default `maxWidth` is `8xl`, which maps to `1440px`.

```tsx
import { Box, Container } from '@gravitational/design-system';

function Example() {
  return (
    <Container>
      <Box />
    </Container>
  );
}
```

## Examples

### Sizes

Use the `maxWidth` prop to change the size of the container.

<Canvas of={ContainerStories.Sizes} />

### Fluid

Use the `fluid` prop to make the container stretch to the full width of its parent.

<Canvas of={ContainerStories.Fluid} />

## Props

<PropTypes of={Container} />
